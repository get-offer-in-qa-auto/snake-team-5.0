[pytest]
addopts =
    --alluredir=artifacts/allure-results
    --clean-alluredir
norecursedirs =
    teamcity-local
    artifacts
markers =
    smoke: minimal checks that verify the TeamCity stand is basically available
    regression: broader checks that verify TeamCity behavior on the selected test stand
