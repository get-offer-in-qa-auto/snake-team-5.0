name: TeamCity PostgreSQL Regression

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      parallelism:
        description: Number of pytest workers and TeamCity agents.
        required: true
        default: "2"
        type: choice
        options:
          - "1"
          - "2"
          - "3"

concurrency:
  group: teamcity-postgresql-regression-${{ github.ref }}
  cancel-in-progress: false

jobs:
  postgresql-regression:
    name: Run TeamCity PostgreSQL regression
    runs-on: ubuntu-latest
    env:
      TEAMCITY_PARALLELISM: ${{ inputs.parallelism || vars.TEAMCITY_PARALLELISM || '2' }}
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate PostgreSQL CI credentials
        shell: bash
        run: |
          password="$(openssl rand -hex 24)"
          dsn="postgresql://teamcity:${password}@localhost:55432/teamcity"
          echo "::add-mask::$password"
          echo "::add-mask::$dsn"
          echo "TEAMCITY_POSTGRES_PASSWORD=$password" >> "$GITHUB_ENV"
          echo "TEAMCITY_DB_ADAPTER=postgresql" >> "$GITHUB_ENV"
          echo "TEAMCITY_DB_DSN=$dsn" >> "$GITHUB_ENV"

      - name: Start and prepare TeamCity with PostgreSQL
        uses: ./.github/actions/teamcity-setup
        with:
          compose_file: ci/teamcity/postgresql/compose.yaml
          server_container: teamcity-server-postgresql-ci
          agent_count: ${{ env.TEAMCITY_PARALLELISM }}
          database_mode: external
          database_preflight: "true"

      - name: Run TeamCity PostgreSQL smoke gate
        uses: ./.github/actions/teamcity-test
        with:
          pytest_marker: smoke
          pytest_workers: ${{ env.TEAMCITY_PARALLELISM }}
          junit_filename: smoke-teamcity-postgresql-regression.xml
          server_container: teamcity-server-postgresql-ci

      - name: Run remaining TeamCity PostgreSQL regression tests
        uses: ./.github/actions/teamcity-test
        with:
          pytest_marker: "(regression) and not smoke"
          pytest_workers: ${{ env.TEAMCITY_PARALLELISM }}
          junit_filename: teamcity-postgresql-regression.xml
          server_container: teamcity-server-postgresql-ci
          preserve_allure_results: "true"

      - name: Build PostgreSQL reports and stop TeamCity
        id: teamcity-report
        if: always()
        uses: ./.github/actions/teamcity-report
        with:
          test_label: postgresql-regression
          compose_file: ci/teamcity/postgresql/compose.yaml
          server_container: teamcity-server-postgresql-ci
          logs_artifact_name: teamcity-postgresql-regression-logs
          results_artifact_name: teamcity-postgresql-regression-test-results
          page_artifact_name: teamcity-postgresql-login-page
