# Backlog улучшений автотестов и CI/CD

Дата первичного ревью: 2026-07-16  
Статус документа: живой чеклист

Этот документ фиксирует замечания по автотестам TeamCity и GitHub Actions.
После исправления пункта нужно:

1. заменить `[ ]` на `[x]`;
2. указать дату исправления;
3. добавить ссылку на PR или commit;
4. коротко записать, как проверено исправление.

## Сводка

- [ ] P0 — 1 из 3 замечаний исправлено
- [ ] P1 — 7 замечаний
- [ ] P2 — 8 замечаний

## P0 — критичные

### [x] TC-AUTO-001 — Исправить авторизацию TeamCity Backup API

Статус: исправлено и проверено в GitHub Actions на `main`.

Последний regression завершился с результатом `9 failed / 30 passed`: DB-тесты
получили `401 Unauthorized` при вызове `POST /app/rest/server/backup`.

Затронутые места:

- `src/main/api/database/client.py`;
- `src/main/api/specs/request_specs.py`;
- `scripts/teamcity_start_smoke.py`;
- `.github/actions/teamcity-pytest/action.yml`.

Что сделать:

- [x] создать для CI явного администратора и access token с необходимыми правами;
- [x] безопасно передать credentials тестам;
- [x] не использовать неизвестный тестам случайный пароль после bootstrap;
- [x] добавить preflight-проверку Backup API до запуска DB-suite;
- [x] сохранять TeamCity auth/rest/server logs для диагностики без утечки token.

Критерий готовности:

- все DB-тесты проходят в GitHub Actions;
- полный regression завершён успешно минимум два раза подряд;
- при некорректных credentials pipeline падает на понятной preflight-проверке.

Пункт закрыт по целевой проблеме авторизации: Backup API preflight успешно
выполнился в двух последовательных прогонах, `401 Unauthorized` не повторился.
Второй полный regression завершился как `1 failed / 38 passed` из-за отсутствия
`build_type_mapping` в параллельно снятом snapshot. Этот отдельный дефект относится
к `TC-AUTO-008` и не связан с credentials или доступом к Backup API.

Исходный проблемный run:
https://github.com/get-offer-in-qa-auto/snake-team-5.0/actions/runs/29503152005

Локальная проверка: 2026-07-16 — чистый TeamCity bootstrap, Bearer preflight и
полный `pytest -m regression -n 3`: `39 passed`.  
Исправлено: 2026-07-16

PR/commit: https://github.com/get-offer-in-qa-auto/snake-team-5.0/pull/35,
merge commit `1bfe241`

Проверка:

- https://github.com/get-offer-in-qa-auto/snake-team-5.0/actions/runs/29508288127 —
  DB preflight passed, `39 passed`;
- https://github.com/get-offer-in-qa-auto/snake-team-5.0/actions/runs/29508647065 —
  DB preflight passed, авторизационных ошибок нет; `1 failed / 38 passed` относится
  к `TC-AUTO-008`.

### [ ] TC-AUTO-002 — Запускать достаточный набор тестов до merge

В pull request запускаются только 5 smoke-тестов из 39. Изменения DB-адаптера
прошли smoke, но сломали ручной regression после merge.

Что сделать:

- сохранить короткий smoke на каждый PR;
- запускать полный regression для изменений тестового фреймворка, API-клиента,
  фикстур, database adapter и CI bootstrap;
- запускать regression на `main` и по расписанию;
- выделить DB-тесты в отдельный marker/job;
- добавить быстрые unit-тесты до Docker job.

Критерий готовности:

- изменения DB/framework-кода не могут быть смержены без соответствующих тестов;
- regression автоматически запускается на `main` и nightly;
- правила выбора suite описаны в документации.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-003 — Убрать write-права из недоверенного PR-кода

PR job получает `pages: write` и `id-token: write`, а job публикации выполняет
локальный composite action из checkout pull request с `contents: write`.

Что сделать:

- оставить тестовым PR jobs только минимальные read-права;
- вынести публикацию Pages в доверенный `workflow_run`, использующий workflow и
  scripts из default branch;
- не исполнять изменяемый PR-код с `contents: write`, `pages: write` или OIDC;
- проверить поведение для pull request из fork.

Критерий готовности:

- PR-код не выполняется с write-токеном;
- Allure report продолжает публиковаться после завершения тестового workflow;
- fork PR не ломает обязательные status checks из-за отсутствия write-прав.

Исправлено: —  
PR/commit: —  
Проверка: —

## P1 — высокий приоритет

### [ ] TC-AUTO-004 — Сделать readiness-проверку содержательной

Сейчас любой HTTP `200`, а также `401/403`, может считаться признаком готового
TeamCity. Smoke-тест проверяет только флаг `opened`.

Что сделать:

- проверять авторизованный `/app/rest/server`;
- валидировать версию и build number;
- отдельно различать HTTP-доступность, завершённый setup и готовый REST API;
- не считать first-start/setup page успешной готовностью стенда.

Критерий готовности:

- smoke падает на произвольной HTTP-странице и незавершённом setup;
- smoke проходит только после успешного ответа TeamCity REST API.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-005 — Проверять TeamCity Agent и реальный build

Pipeline поднимает тяжёлый TeamCity Agent, но не проверяет его авторизацию,
состояние и способность выполнить build.

Что сделать:

- авторизовать agent в bootstrap;
- проверить состояние `authorized + connected/idle`;
- создать минимальную build configuration;
- запустить build и проверить ожидаемый результат;
- если это не входит в быстрый smoke — не поднимать agent в этом job.

Критерий готовности:

- либо smoke подтверждает полный agent/build lifecycle;
- либо быстрый API-smoke больше не тратит ресурсы на неиспользуемый agent.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-006 — Добавить timeout для всех HTTP-запросов

CRUD-запросы через `requests` выполняются без timeout и могут зависнуть до
общего timeout GitHub Actions job.

Что сделать:

- определить общий connect/read timeout в конфигурации;
- передавать timeout во все `get/post/put/delete`;
- добавить понятную диагностику timeout;
- покрыть timeout unit-тестами.

Критерий готовности:

- в API-клиенте нет HTTP-запросов без явно заданного timeout;
- зависший endpoint приводит к ограниченной и диагностируемой ошибке.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-007 — Сделать cleanup надёжным

Созданный объект может не попасть в cleanup, если после POST упала валидация или
дополнительный API-вызов. Первая ошибка удаления также останавливает очистку.

Что сделать:

- регистрировать ресурс сразу после успешного создания;
- сделать удаление идемпотентным;
- выполнять cleanup всех объектов best-effort;
- агрегировать ошибки cleanup, не скрывая исходное падение теста;
- добавить тесты на частично созданные ресурсы и повторное удаление.

Критерий готовности:

- после намеренно упавшего теста созданные ресурсы удаляются;
- одна ошибка cleanup не мешает удалить остальные объекты;
- исходная ошибка теста остаётся видимой в отчёте.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-008 — Изолировать DB-тесты от параллельного CRUD

Backup-снимки базы запускаются вперемешку с stateful API-тестами через xdist.

Что сделать:

- добавить pytest marker `database`;
- запускать DB-suite отдельным последовательным job либо xdist group;
- добавить polling для eventually consistent create/delete операций;
- оценить переход CI DB-проверок на PostgreSQL adapter вместо backup на каждый
  assertion.

Критерий готовности:

- DB-проверки воспроизводимо проходят минимум пять раз подряд;
- параллельные CRUD-тесты не изменяют состояние во время проверяемого snapshot;
- время DB-suite остаётся предсказуемым.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-009 — Разделить base URL и health URL

`TEAMCITY_URL` используется и как полный URL страницы login, и как base URL REST
API. Значение с `/login.html` приводит к некорректному REST URL.

Что сделать:

- использовать `TEAMCITY_BASE_URL` для API;
- использовать отдельный `TEAMCITY_HEALTH_URL` для readiness;
- нормализовать и валидировать URL при старте тестов;
- обновить документацию и примеры запуска.

Критерий готовности:

- API URL не зависит от пути readiness endpoint;
- неправильное значение завершается ранней понятной ошибкой.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-010 — Добавить unit-тесты тестового фреймворка

Сейчас CI проверяет фреймворк почти исключительно через дорогой Docker e2e.

Что покрыть:

- readiness classification и bootstrap transitions;
- request/response specs и timeout;
- cleanup lifecycle;
- random model generator;
- model comparator и config loader;
- database snapshots/adapters с mock/fake transport;
- Allure Pages history scripts;
- Telegram notification parser.

Критерий готовности:

- отдельный быстрый unit job запускается до Docker;
- ключевые ошибочные ветви покрыты тестами;
- unit job выполняется за разумное время без TeamCity стенда.

Исправлено: —  
PR/commit: —  
Проверка: —

## P2 — средний приоритет

### [ ] TC-AUTO-011 — Зафиксировать зависимости и инструменты CI

Что сделать:

- зафиксировать Python dependencies через lock/constraints;
- задать конкретную версию Allure CLI;
- закрепить GitHub Actions по commit SHA;
- контролируемо обновлять Docker image и при необходимости использовать digest;
- убрать неиспользуемые зависимости из API-only окружения.

Критерий готовности:

- повторный pipeline устанавливает одинаковые версии;
- обновления зависимостей выполняются отдельным PR.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-012 — Ускорить и удешевить pipeline

Что сделать:

- включить pip cache;
- добавить `paths-ignore` или выбор jobs по затронутым файлам;
- добавить PR concurrency с `cancel-in-progress: true`;
- не устанавливать Playwright в API-only job;
- не запускать неиспользуемые контейнеры;
- измерять длительность основных этапов.

Критерий готовности:

- повторные коммиты отменяют устаревший PR run;
- документационные изменения не поднимают TeamCity без необходимости;
- время и объём скачиваемых данных заметно уменьшены.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-013 — Ограничить рост Allure history в gh-pages

Что сделать:

- определить retention для отчётов каждой suite;
- автоматически удалять старые каталоги;
- хранить trend history отдельно от полных HTML reports либо использовать sparse
  checkout;
- не клонировать всю накопленную Pages-ветку ради одного history-каталога.

Критерий готовности:

- число сохранённых отчётов ограничено;
- размер gh-pages и время restore history не растут бесконечно.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-014 — Удалить или восстановить неработающий UI setup hook

`src/main/api/fixtures/setup_hook.py` импортирует отсутствующие UI-модули, не
подключён в `tests/conftest.py` и исключён из mypy.

Критерий готовности:

- мёртвый код удалён либо полностью восстановлен;
- файл снова проверяется mypy и тестами;
- Playwright находится только в UI-наборе зависимостей.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-015 — Усилить валидацию моделей и API-контракта

Что сделать:

- превратить generation regex в реальные ограничения request-моделей;
- определить обязательные response fields для критичных сценариев;
- не скрывать несовпадения типов сравнением через `str()`;
- проверить корректность response model для списка пользователей;
- добавить boundary/negative cases для полей, а не только случайные happy-path
  значения.

Критерий готовности:

- неверные типы и значения выявляются до отправки запроса или при разборе ответа;
- contract regression приводит к понятному падению теста.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-016 — Ужесточить pytest и type-checking конфигурацию

Что сделать:

- добавить `testpaths`;
- включить `--strict-markers` и `--strict-config`;
- проверять, что каждый тест входит в ожидаемую suite;
- постепенно включить mypy для tests и scripts;
- включить проверку тел неаннотированных функций либо добавить аннотации.

Критерий готовности:

- опечатка в marker останавливает CI;
- новый тест нельзя случайно исключить из всех suite;
- tests/scripts участвуют в статической проверке.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-017 — Пересмотреть branch protection

Текущая защита `main` использует `strict=false`, не требует approval и включает
публикацию Allure Pages как обязательный status check.

Что сделать:

- сделать обязательными Code Quality и тестовые jobs;
- не использовать публикацию отчёта как quality gate;
- рассмотреть требование актуальности branch относительно `main`;
- при необходимости команды включить минимум один approval.

Критерий готовности:

- merge блокируется только содержательными quality checks;
- инфраструктурная ошибка публикации отчёта не подменяет результат тестов;
- политика review явно согласована командой.

Исправлено: —  
PR/commit: —  
Проверка: —

### [ ] TC-AUTO-018 — Актуализировать документацию

Документация всё ещё называет bootstrap следующим этапом и содержит устаревшее
описание параметров ручного workflow.

Что сделать:

- синхронизировать README и `docs/github-actions-pipeline.md` с реализацией;
- описать актуальные smoke/regression/database jobs;
- описать credentials, URL variables, Pages publishing и retention;
- добавить ссылку на этот backlog.

Критерий готовности:

- команды и схема pipeline в документации соответствуют текущим workflow;
- новый участник может воспроизвести локальный и CI-запуск без устных пояснений.

Исправлено: —  
PR/commit: —  
Проверка: —

## История обновлений

| Дата | Изменение | PR/commit |
| --- | --- | --- |
| 2026-07-16 | Создан backlog по результатам ревью автотестов и CI/CD | — |
| 2026-07-16 | Для TC-AUTO-001 подготовлены runtime admin, Bearer token и DB preflight; локальный regression: 39 passed | — |
