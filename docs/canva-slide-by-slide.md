# ИНСТРУКЦИИ ДЛЯ CANVA — НЕ ПОКАЗЫВАТЬ В ПРЕЗЕНТАЦИИ

Используй этот документ как единственный источник содержания.

Каждый раздел с пометкой `SLIDE` — отдельный слайд презентации. Сохраняй
указанный порядок и размещай текст только на назначенном ему слайде. Не
объединяй разные слайды.

Не придумывай, не переписывай, не пересказывай, не сокращай, не удаляй и не
расширяй предоставленный русский текст. Сохраняй имена, технические термины,
числа и обозначения STAR точно в исходном виде. Твоя задача — только визуальное
оформление.

Если текст не помещается, создай дополнительный слайд-продолжение, не удаляя
содержание. Не добавляй unit-тесты, contract-тесты, технологии, метрики,
достижения или выводы, которых нет в документе.

Не показывай эти инструкции в готовой презентации.

---

# SLIDE 1

## Автоматизация TeamCity с нуля

### Командные достижения и личный вклад

**QA-команда**

Екатерина Кончина

Пётр Самохин

Никита Снегирев

---

# SLIDE 2

## Проект начинался с нуля

На старте отсутствовали:

- готовый набор автотестов;
- согласованный тестовый план;
- единая структура автоматизации;
- воспроизводимый CI-запуск;
- автоматически подготовленное окружение TeamCity;
- единый способ анализа результатов.

**Цель команды:** построить систему автоматизированного контроля качества
TeamCity — от тестового плана до CI и отчётности.

---

# SLIDE 3

## Командный кейс 1. Автоматизация ключевых сценариев TeamCity

### Задача

С нуля создать regression ключевых функций TeamCity на нескольких уровнях.

### Что сделала команда

- сформировала тестовый план и scope;
- реализовала API-проверки проектов, configurations, build steps, пользователей
  и access tokens;
- реализовала UI-сценарии входа и управления основными сущностями;
- добавила DB- и configuration-проверки;

### Результат

**50 API-проверок + 7 UI-сценариев × 3 браузера.**

Это 71 результат выполнения, а не 71 уникальный сценарий. Unit-тесты и отдельный
contract regression в заявленный набор не входят.

---

# SLIDE 4

## Командный кейс 2. Автоматизация запуска в GitHub Actions

### Задача

Настроить запуск проверок в GitHub Actions с автоматическим развёртыванием и
первичной настройкой отдельного чистого TeamCity-стенда.

### Что сделала команда

- автоматизировала развёртывание и первоначальную настройку TeamCity;
- разделила API и три браузера на независимые jobs и стенды;
- настроила последовательность: проверка БД → smoke → полный regression;
- вынесла PostgreSQL regression в отдельный регулярный и ручной запуск.

### Результат

Запуск не зависит от заранее подготовленного стенда. API и три браузера работают
независимо, а критические падения становятся quality gate.

---

# SLIDE 5

## Командный кейс 3. Единая отчётность в Allure и QA-дашбордах

### Задача

Сделать результаты распределённого прогона понятными всей команде и сохранить
историю и диагностику.

### Что сделала команда

- объединила результаты API и трёх браузеров в один Allure-отчёт;
- сделала отчёт понятным: сценарии, шаги, браузеры и причины падений;
- настроила историю запусков, постоянные ссылки, логи и диагностику;
- добавила QA-дашборд с метриками прогонов и отдельный dashboard code coverage.

### Результат

Команда видит сценарий, браузер, шаг и причину падения, сравнивает результаты в
динамике и переходит к диагностике конкретного запуска.

---

# SLIDE 6

### Проблемы, с которыми команда столкнулась во время работы над проектом


---

# SLIDE 7

## Проблема 1. TeamCity останавливался на первом запуске

### Что произошло

На чистом runner обычного запуска Docker-контейнера оказалось недостаточно.
TeamCity требовал подтверждения first start, выбора БД, принятия лицензии,
создания администратора, access token и авторизации Agents.

### Почему это глобальная проблема

До завершения setup невозможно выполнить ни API-, ни UI-, ни DB-проверки.
Проблема блокировала весь CI-контур.

### Решение команды

Команда автоматизировала первоначальную настройку TeamCity: подтверждение первого
запуска, выбор базы данных, принятие лицензии, создание временного администратора
и ключа доступа. Перед тестами система проверяет, что TeamCity полностью настроен
и готов к работе.

### Результат

В каждом CI job автоматически разворачивается и настраивается отдельный чистый
TeamCity-стенд; ручное вмешательство не требуется.

---

# SLIDE 8

## Проблема 2. Параллельные тесты влияли друг на друга

### Что произошло

При параллельном запуске несколько API- и UI-тестов одновременно создавали,
изменяли и удаляли данные на одном TeamCity-стенде. Один тест мог перезаписать
или удалить данные, которые в этот момент использовал другой. Это создавало
ложные падения и делало результаты нестабильными и невалидными.

### Решение команды

- выделила отдельный чистый TeamCity-стенд для API и каждого браузера;
- изолировала тестовые данные с помощью уникальных имён и идентификаторов;
- закрепила за каждым тестом создание и удаление собственных объектов;
- согласовала количество параллельных тестов с количеством TeamCity Agents.

### Результат

Тесты выполняются параллельно, но не меняют данные друг друга. Результаты стали
стабильными и воспроизводимыми: падение указывает на реальную проблему, а не на
конфликт между одновременно работающими тестами.

---

# SLIDE 9

## Проблема 3. Невоспроизводимость тестового окружения

### Что произошло

Локальные запуски и запуски на чистых CI runners могли выполняться в разных
условиях. Результат зависел от версии и состояния TeamCity, настроек базы данных,
credentials, доступности Agents и готовности сервисов. Один и тот же набор тестов
мог проходить на заранее подготовленном стенде и падать после развёртывания с
нуля.

### Почему это глобальная проблема

Различия окружений влияли одновременно на API-, UI- и DB-проверки. Команда не
могла однозначно определить, связано ли падение с дефектом TeamCity, тестом или
конфигурацией стенда.

### Решение команды

Команда зафиксировала конфигурацию окружения в Docker Compose и CI actions,
централизовала параметры запуска и credentials, автоматизировала настройку
TeamCity и добавила healthcheck, preflight и проверки готовности Server, Agents и
базы данных. Для API и каждого браузера поднимается отдельный чистый стенд.

### Результат

На чистом runner воспроизводится одинаковый порядок развёртывания и настройки
окружения. Запуск больше не зависит от вручную подготовленного состояния, а
результаты разных jobs можно сравнивать между собой.

---

# SLIDE 10

## Личный вклад Екатерины Кончиной

### Три самостоятельных направления

1. Построение CI-пайплайна с Telegram-уведомлениями.
2. Независимая DB-верификация состояния TeamCity.
3. Создание системы отчётности: Allure, QA-метрики и coverage.

Каждый кейс представлен по методологии STAR:

**Situation → Task → Action → Result**

---

# SLIDE 11

## Личный кейс 1. CI-пайплайн и Telegram-уведомления

### S — Situation

Изначально автотесты запускались только локально на компьютерах участников
команды. Результат зависел от настроенного окружения конкретного человека, а
автоматического запуска при изменениях в проекте и уведомлений не было.

### T — Task

Построить полный CI-процесс: от автоматического развёртывания TeamCity и запуска
проверок до понятного уведомления команды о результате.

### A — Action

Я настроила GitHub Actions pipeline с проверкой кода, автоматической подготовкой
TeamCity, smoke gate, API- и кроссбраузерным regression. Также разработала
Telegram-уведомления со статусом jobs, данными PR и ссылками на Allure и запуск.

### R — Result

Каждый PR проходит воспроизводимую проверку на чистом стенде, а команда сразу
получает в Telegram итог прогона и ссылки для анализа ошибок.

---

# SLIDE 12

## Личный кейс 2. Независимая DB-верификация

### S — Situation

Успешный REST-ответ не доказывал, что изменения действительно сохранились во
внутреннем состоянии TeamCity. При этом прямое подключение к работающей HSQLDB
было небезопасным.

### T — Task

Создать единый безопасный механизм проверки данных для HSQLDB и PostgreSQL без
SQL внутри самих тестов.

### A — Action

Я разделила DB-слой на запросы, исполнители и клиент; реализовала безопасное
чтение HSQLDB через backup snapshot и read-only подключение к PostgreSQL; добавила
проверки сохранения и удаления основных сущностей и database preflight.

### R — Result

API-проверки получили независимое подтверждение на уровне базы данных. Одна
тестовая логика работает с HSQLDB и PostgreSQL, не вмешиваясь в работу TeamCity.

---

# SLIDE 13

## Личный кейс 3. Система отчётности и QA-метрик

### S — Situation

Результаты API и трёх браузеров формировались отдельно, а один Allure-отчёт не
показывал динамику качества, нестабильные тесты и изменение покрытия.

### T — Task

Создать единую систему, в которой можно увидеть текущий результат, историю
прогонов, метрики стабильности и покрытие API-фреймворка.

### A — Action

Я объединила API, Chromium, Firefox и WebKit в общий Allure-отчёт; настроила
историю и постоянные ссылки; разработала QA metrics и coverage dashboards;
связала показатели с конкретными прогонами.

### R — Result

Команда видит общий результат автоматизации, сравнивает прогоны, находит flaky- и
медленные тесты, контролирует покрытие и переходит от метрики к нужному отчёту.

---

# SLIDE 14

## Конкретные проблемы, решённые Екатериной

### Три технических STAR-кейса

1. CI-стенд с HSQLDB нельзя было переиспользовать для PostgreSQL.
2. DB-клиент для HSQLDB не подходил для работы с PostgreSQL.
3. UI-тесты падали нестабильно, а причину было сложно локализовать.

Формат каждой истории:

**Симптом → задача → исправление → подтверждение результата**

---

# SLIDE 15

## Проблема 1. Для PostgreSQL потребовался отдельный CI-стенд

### S — Situation

Основной CI-стенд запускал TeamCity со встроенной HSQLDB внутри готового
окружения. PostgreSQL требовала отдельного контейнера, собственного хранилища и
сети. Если база не успевала запуститься или TeamCity не получал JDBC-драйвер и
настройки подключения, сервер не стартовал и тесты не выполнялись.

### T — Task

Построить отдельный воспроизводимый стенд TeamCity с PostgreSQL, который можно
поднять на чистом runner без ручной настройки.

### A — Action

Я создала Docker Compose с отдельными контейнерами TeamCity Server, Agents и
PostgreSQL 17.5; добавила healthcheck базы и правильный порядок запуска;
автоматизировала установку JDBC-драйвера, подготовку Data Directory и передачу
временных credentials.

### R — Result

TeamCity с PostgreSQL автоматически разворачивается в отдельном CI-контуре.
Полный API- и UI-regression запускается вручную и по nightly-расписанию, не
увеличивая время обязательных PR-проверок.

---

# SLIDE 16

## Проблема 2. DB-клиент для HSQLDB не работал с PostgreSQL

### S — Situation

DB-проверки для встроенной HSQLDB читали данные из backup snapshot TeamCity.
После подключения PostgreSQL этот механизм нельзя было использовать напрямую:
внешняя база требовала отдельного соединения и другого способа выполнения
запросов. Без нового клиента тесты не могли проверять состояние PostgreSQL.

### T — Task

Добавить поддержку PostgreSQL, не дублируя DB-проверки и не добавляя SQL в сами
тесты.

### A — Action

Я разработала отдельные `PostgreSQLDatabaseClient` и `PostgreSQLExecutor`,
настроила подключение через DSN и перевела транзакции в read-only режим. Фабрика
автоматически выбирает PostgreSQL или backup adapter, а тесты используют общий
`DBRequest` и одинаковые verification steps.

### R — Result

Одни и те же DB-проверки работают с двумя источниками: HSQLDB читается через
безопасный snapshot, а PostgreSQL — через прямое read-only соединение. Тестовую
логику и SQL дублировать не пришлось.

---

# SLIDE 17

## Проблема 3. UI-тесты падали нестабильно

### S — Situation

Сценарий создания Build Step то проходил, то падал в браузерных прогонах.
Build Step уже создавался в TeamCity, но тест начинал следующую проверку до
завершения перехода на страницу Build Steps. Из-за нерегулярного падения
причину пришлось искать в несколько этапов.

### T — Task

Найти источник нестабильности и сделать сценарий предсказуемым без фиксированных
задержек и бесконечного увеличения timeout.

### A — Action

Я проанализировала поведение страницы после нажатия Save, уточнила фактический
URL перенаправления и добавила ожидание целевой страницы. Только после завершения
перехода тест проверяет созданный Build Step.

### R — Result

Сценарий синхронизирован с реальным состоянием TeamCity. Проверка больше не стартует
во время перехода страницы, а UI-regression использует одинаковую логику ожиданий в Chromium,
Firefox и WebKit.

---

# SLIDE 18

## Личный вклад Никиты Снегирева

### Три самостоятельных направления

1. Формирование тестового плана и scope автоматизации.
2. Разработка API regression для ключевых сущностей TeamCity.
3. Создание UI-слоя и автоматизация пользовательских сценариев.

Каждый кейс представлен по методологии STAR:

**Situation → Task → Action → Result**

---

# SLIDE 19

## Личный кейс 1. Тестовый план и scope автоматизации

### S — Situation

Проект начинался с нуля: отсутствовали согласованный набор проверок, приоритеты
автоматизации и единое понимание того, какие возможности TeamCity необходимо
покрыть через API и UI.

### T — Task

Сформировать основу тестового плана, определить границы автоматизации и разложить
ключевые функции TeamCity на конкретные проверяемые сценарии.

### A — Action

Я проанализировал основные сущности и пользовательские процессы TeamCity,
выделил позитивные, негативные и авторизационные проверки, определил приоритеты и
разделил сценарии по уровням API и UI. Результаты зафиксировал в общем тестовом
плане команды.

### R — Result

Команда получила согласованный scope и основу для последовательной реализации
regression. Разработка тестов велась по единой структуре, охватывающей проекты,
build configurations, пользователей, access tokens, роли и права доступа.

---

# SLIDE 20

## Личный кейс 2. API regression ключевых сущностей TeamCity

### S — Situation

В проекте существовал базовый каркас API-фреймворка, но не было системного набора
проверок жизненного цикла сущностей, негативных сценариев и разграничения прав
доступа.

### T — Task

Расширить API-автоматизацию и создать переиспользуемые проверки основных функций
TeamCity.

### A — Action

Я разработал DTO, fixtures, endpoints и API steps для проектов, build
configurations, пользователей, access tokens, ролей и permissions. Реализовал
проверки создания, получения, изменения и удаления сущностей, негативные и
авторизационные сценарии, генерацию тестовых данных и последующую очистку
созданных объектов.

### R — Result

Проект получил полноценный API regression для ключевых сущностей TeamCity. Общие
модели, fixtures и steps стали переиспользуемой основой для дальнейшего
расширения набора автотестов.

---

# SLIDE 21

## Личный кейс 3. UI-слой и пользовательские сценарии

### S — Situation

API-проверки подтверждали работу серверной части, но не проверяли реальные
действия пользователя в интерфейсе TeamCity. Отдельного UI-слоя, Page Objects и
общего механизма браузерной авторизации не было.

### T — Task

Создать масштабируемую основу UI-автоматизации и покрыть ключевые пользовательские
сценарии TeamCity.

### A — Action

Я построил UI-слой на Playwright: разработал BasePage, Page Objects, общие
компоненты, маршруты, fixtures и механизм управления пользовательскими сессиями.
Автоматизировал вход, создание проектов и пользователей, удаление пользователей,
создание access tokens и проверку ограничений для пользователя с ограниченными
правами.

### R — Result

В проекте появился переиспользуемый UI-фреймворк, а основные пользовательские
операции вошли в общий regression. Новые UI-сценарии можно добавлять через
готовые Page Objects, fixtures и механизмы авторизации.

---

# SLIDE 22

## Конкретные проблемы, решённые Никитой

### Три технических STAR-кейса

1. UI-тестам требовалась авторизация под пользователями с разными ролями.
2. Объекты, созданные через UI, могли оставаться после завершения тестов.
3. Успешное действие в интерфейсе не подтверждало сохранение данных в TeamCity.

Формат каждой истории:

**Симптом → задача → исправление → подтверждение результата**

---

# SLIDE 23

## Проблема 1. UI-тестам требовались сессии разных пользователей

### S — Situation

Для проверки permissions UI-тесты должны были выполняться как от имени
администратора, так и от имени пользователей с ограниченными правами. Повторная
авторизация через форму входа усложняла сценарии, дублировала шаги и связывала
проверку прав с работой страницы логина.

### T — Task

Создать единый механизм авторизации для UI-тестов и обеспечить изоляцию
пользовательской сессии в каждом сценарии.

### A — Action

Я разработал `UiAuthClient`, который получает `TCSESSIONID` по credentials
пользователя и добавляет cookie в Playwright context. Для выбора сессии добавил
markers `admin_session` и `user_session`, а временное состояние пользователей
изолировал через `SessionStorage`.

### R — Result

UI-тесты могут декларативно запускаться с нужной ролью без повторения шагов
входа. Каждый сценарий получает отдельную управляемую сессию, а проверки
permissions стали короче и предсказуемее.

---

# SLIDE 24

## Проблема 2. UI-тесты оставляли созданные данные

### S — Situation

Проекты и пользователи, созданные непосредственно через интерфейс, не проходили
через стандартные API-fixtures. Из-за этого механизм очистки мог не знать о них,
а оставшиеся данные влияли на повторные и параллельные запуски.

### T — Task

Подключить сущности, создаваемые через UI, к общей системе регистрации и очистки
тестовых данных.

### A — Action

Я добавил marker `entity_will_be_created`, который регистрирует модель будущей
сущности в `CreatedObjectsRegistry` до выполнения действия в браузере. Также
расширил cleanup для проектов, пользователей, access tokens и связанных
объектов.

### R — Result

Объекты, созданные через UI, автоматически удаляются после теста. Повторные
запуски начинаются без оставшихся данных, а риск конфликтов между сценариями
снизился.

---

# SLIDE 25

## Проблема 3. UI-проверка не подтверждала сохранение данных

### S — Situation

Сообщение об успехе или появление элемента на странице не гарантировало, что
TeamCity действительно сохранил изменения во внутреннем состоянии. UI-тест мог
пройти, даже если данные отображались временно или сохранились некорректно.

### T — Task

Добавить независимую проверку результата ключевых UI-операций.

### A — Action

Я связал UI-сценарии с `ApiManager`: после создания проекта или пользователя тест
получает сущность через REST API и сравнивает её с исходными данными. Созданный
access token проверяется через API, а после удаления пользователя дополнительно
подтверждается его отсутствие.

### R — Result

UI-тесты проверяют не только отображение результата в браузере, но и фактическое
состояние TeamCity. Ложноположительные результаты исключены, а место ошибки легче
определить по уровню, на котором разошлись данные.
